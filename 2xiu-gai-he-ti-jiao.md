# 2.修改和提交


```git
git status    #查看状态
git diff      #查看更变内容
git add .     #跟踪所有改动过的文件
git add <file>    #跟踪指定的文件
git mv <old> <new>    #文件改名
git rm <file>         # 删除文件
git rm --cached <file>    #停止所有更新过的文件
git commit --amend        #修改最后一次提交
```

