# 7.远程操作
```git
git remote -v    #查看远程版本库信息
git remote show <remote>    #查看指定远程版本信息
git remote add <remote> <url>    #添加远程版本库
git fetch <remote>     #从远程库获取代码
git pull <remote> <branch>     #下载代码以及快速合并

git push <remote> :<branch/tag-name>     #删除远程分支或代码

```